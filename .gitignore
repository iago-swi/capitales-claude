node_modules/
dist/
*.log

# SQLite database — derived state, rebuilt by `npm run seed`
.data/

*.tsbuildinfo

# Downloaded Natural Earth sources; the ETL *outputs* are committed
packages/data/.cache/

# electron-builder output
release/

# Note: apps/desktop/assets/icon.ico, icon.png and icons/ ARE committed.
# They are generated from the SVGs by `npm run icons`, but committing them
# keeps packaging free of a native rasteriser dependency — the same reasoning
# that keeps the ETL outputs in the repository.
